Flask
gunicorn>=25.1
watchdog
apprise

# kcc-c2e's runtime deps, mirrored from KCC's requirements-docker.txt.
# KCC itself is installed with --no-deps in the Dockerfile because its
# install_requires drags in PySide6 (the Qt GUI), which the CLI never uses.
Pillow>=11.3.0
psutil>=5.9.5
requests>=2.31.0
python-slugify>=8.0.4
packaging>=23.2
mozjpeg-lossless-optimization>=1.2.0
natsort>=8.4.0
distro>=1.8.0
numpy>=1.22.4
PyMuPDF>=1.16.1
