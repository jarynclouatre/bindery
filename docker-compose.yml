services:
  ereader-converter:
    build: .
    container_name: ereader-converter
    ports:
      - "5000:5000"
    environment:
      - PUID=1000
      - PGID=1000
    volumes:
      - ./config:/app/config
      - ./books_in:/Books_in
      - ./books_out:/Books_out
      - ./comics_in:/Comics_in
      - ./comics_out:/Comics_out
    restart: unless-stopped
